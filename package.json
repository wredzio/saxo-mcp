{
  "name": "saxo-mcp",
  "version": "0.1.0",
  "description": "Saxo Bank MCP server (stdio)",
  "main": "src/stdio.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/stdio.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/node": "^24.2.0",
    "bun-types": "latest",
    "typescript": "^5.9.2"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
